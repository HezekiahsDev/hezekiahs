import Navbar from "@/app/components/Navbar";
import HeroSection from "@/app/components/HeroSection";

const Home = () => {
  return (
    <div>
      <Navbar />
      <HeroSection />
      {/* Add more sections here (e.g., About, Portfolio, Contact) */}
    </div>
  );
};

export default Home;
